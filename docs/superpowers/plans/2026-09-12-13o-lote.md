# 13º em lote (Bloco 8c) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pagar o 13º salário pelo ERP, em lote por ano e parcela, gerando conta a pagar por colaborador e guia por grupo de recolhimento.

**Architecture:** Espelha a folha (`folhas` / `folha_itens`) na estrutura e a rescisão (`rh_rescisoes`) no ciclo de vida. A conta de dinheiro vive inteira em `fn_gerar_decimo_terceiro` (SQL), provada por `supabase/provas/decimo_terceiro.sql` contra números calculados à mão. O TypeScript valida entrada, agrega para a tela e não recalcula dinheiro.

**Tech Stack:** Next.js 15 App Router, TypeScript strict, Supabase (Postgres + RLS), Zod v4, vitest, shadcn.

**Spec:** `docs/superpowers/specs/2026-09-11-13o-e-ferias-design.md`

**Escopo deste plano:** infra comum + 13º (Bloco 8c). Férias (8d) é o plano seguinte e reusa o caminho de imposto e de lançamento provado aqui.

## Global Constraints

- **Migrations vão pelo MCP `apply_migration`**, no projeto vivo `vsesgvqjgqpapoxhnbqx`, com `name` igual ao nome completo do arquivo. `supabase db push` é bomba armada neste repo: NUNCA rodar. O `.sql` versionado fica em `supabase/migrations/`.
- **Reler a definição viva antes de alterar qualquer função existente.** O arquivo do repo diverge do banco em 155 versões. Copiar a definição de `pg_get_functiondef`, alterar a partir dela, e diffar depois esperando só a mudança prevista.
- **`timestamp` sem fuso** para hora de parede; `timestamptz` só onde o padrão da tabela vizinha já usa.
- **Dinheiro é `numeric(14,2)`.** Dividir antes de multiplicar, e o `round` envolve a divisão.
- **Testes:** sandbox desligado. **NÃO passar `--environment=node` na suíte inteira**: o `vitest.config.ts` define `environment: "jsdom"` e o override derruba todo teste de componente com `document is not defined`. Medido em 12/09/2026: a suíte inteira com o flag deu **458 falhas e 71 arquivos quebrados**, e ainda assim **saiu com exit code 0**. O flag só serve para rodar um arquivo que é puro de node. Comando da suíte: `npx vitest run`.
- **Exit 0 não é suíte verde.** Ler a linha `Test Files`/`Tests` e comparar com o baseline, sempre.

## Baseline medido em 12/09/2026 (antes de eu acrescentar teste)

| Medida | Valor |
|---|---|
| `npx vitest run` | **2984 testes: 2980 passam, 4 falham** em 224 arquivos |
| `npx tsc --noEmit` | 1 erro |

As 4 falhas e o 1 erro de tipo vêm **inteiramente de dois arquivos não rastreados de outra sessão**: `src/modules/compras/ordens/__repro.test.ts` e `src/modules/compras/ordens/components/__repro-editar.test.tsx`. Rodados sozinhos dão exatamente 2 arquivos e 4 testes falhando.

Não são meus, não entram no PR (são `??` no git) e o CI não os vê. **Não apagar**: é trabalho de outra frente. Na Task 10, verificar excluindo esses dois e dizer isso explicitamente no relato, em vez de anunciar suíte verde por cima deles.
- **Valor em real nos testes é asserido pelo formatador**, nunca por string literal: o separador do `pt-BR` não é espaço comum.
- **Commits frequentes**, um por task. Branch `decimo-terceiro-e-ferias`, já criada a partir de `origin/main`.
- **`git fetch origin` antes de cada task**: outra frente pode ter mexido no banco ou no main.

## File Structure

**Migrations (novas):**
- `supabase/migrations/20260912<hhmmss>_origens_13o_e_recurso.sql` — abre o CHECK de `lancamentos.origem` e renomeia o recurso `rh.ferias`.
- `supabase/migrations/20260912<hhmmss>_decimo_terceiro_tabelas.sql` — as duas tabelas, RLS, audit.
- `supabase/migrations/20260912<hhmmss>_decimo_terceiro_gerar.sql` — `fn_gerar_decimo_terceiro` e helpers.
- `supabase/migrations/20260912<hhmmss>_decimo_terceiro_ciclo.sql` — editar item, enviar, rejeitar, excluir.
- `supabase/migrations/20260912<hhmmss>_decimo_terceiro_aprovar.sql` — aprovar e desaprovar.

**Prova:**
- `supabase/provas/decimo_terceiro.sql` — números à mão no cabeçalho, RPCs chamadas, `raise exception` no fim.

**Módulo (novo):** `src/modules/rh/decimo-terceiro/`
- `schemas.ts` — validação de entrada (gerar, editar item, motivo).
- `queries.ts` — leitura (`import "server-only"`).
- `calculo.ts` — agregação pura para a tela. **Não calcula dinheiro.**
- `calculo.test.ts`, `schemas.test.ts`
- `actions.ts` — server actions.
- `components/lotes-tabela.tsx`, `components/gerar-lote-drawer.tsx`, `components/lote-detalhe.tsx`, `components/excluidos-do-lote.tsx`, `components/editar-item-drawer.tsx`

**Rotas:**
- Criar: `src/app/(app)/rh/decimo-terceiro-e-ferias/page.tsx`
- Criar: `src/app/(app)/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx`
- Modificar: `src/app/(app)/rh/ferias/page.tsx` — vira redirect permanente.
- Modificar: `src/config/recursos.ts:344-350` — o recurso renomeado.

---

### Task 1: Abrir as origens e renomear o recurso

Vai ao ar sem mudar comportamento nenhum. É a fundação das outras tasks.

**Files:**
- Create: `supabase/migrations/20260912<hhmmss>_origens_13o_e_recurso.sql`
- Modify: `src/config/recursos.ts:344-350`
- Test: `src/config/recursos.test.ts`

**Interfaces:**
- Produces: a origem `'decimo_terceiro'` aceita em `lancamentos.origem`; o recurso `rh.decimo-terceiro-ferias` com ações `CRUD_APROVA`.

- [ ] **Step 1: Conferir o estado de partida e guardar os números**

```bash
git fetch origin && git status --short
```

Rodar via MCP `execute_sql` e ANOTAR os três números, que são a linha de controle da Step 5:

```sql
select
  (select count(*) from perfil_permissoes  where recurso = 'rh.ferias') as perfil,
  (select count(*) from usuario_permissoes where recurso = 'rh.ferias') as usuario,
  (select count(*) from usuario_permissoes where recurso = 'rh.decimo-terceiro-ferias') as ja_existe;
```

Esperado em 12/09/2026: `perfil = 9`, `usuario = 24`, `ja_existe = 0`. Se vier diferente, PARAR: outra frente mexeu nas permissões e os números da prova mudaram.

- [ ] **Step 2: Escrever a migration**

```sql
-- Abre duas origens novas em lancamentos e renomeia o recurso de férias.
-- Origem nova é pré-requisito das RPCs do 13º: sem ela o insert do lançamento
-- estoura no CHECK, e estoura DEPOIS de o lote já estar marcado como aprovado.
alter table public.lancamentos drop constraint lancamentos_origem_check;

alter table public.lancamentos add constraint lancamentos_origem_check
  check (origem in ('oc','manual','diaria','folha','folha_guia',
                    'adiantamento','rescisao','decimo_terceiro','ferias'));

-- O recurso muda de nome junto com a rota. As DUAS tabelas, porque
-- getUsuarioLogado lê usuario_permissoes (permissão EFETIVA), não
-- perfil_permissoes: renomear só uma tira o acesso de todo mundo na prática.
update public.perfil_permissoes
   set recurso = 'rh.decimo-terceiro-ferias'
 where recurso = 'rh.ferias';

update public.usuario_permissoes
   set recurso = 'rh.decimo-terceiro-ferias'
 where recurso = 'rh.ferias';

-- Ações novas do recurso (aprovar/desaprovar) para o perfil Admin.
-- Em rh.encargos o catálogo declarou `excluir` e NENHUM perfil ganhou,
-- nem o Admin, e o conserto ficou inalcançável pela tela. Molde do
-- conserto: 20260727140001.
insert into public.perfil_permissoes (perfil_id, recurso, acao)
select p.id, 'rh.decimo-terceiro-ferias', a.acao
  from public.perfis p
 cross join (values ('aprovar'), ('desaprovar')) as a(acao)
 where p.nome = 'Admin'
on conflict do nothing;

insert into public.usuario_permissoes (usuario_id, recurso, acao)
select u.id, 'rh.decimo-terceiro-ferias', a.acao
  from public.usuarios u
  join public.perfis p on p.id = u.perfil_id
 cross join (values ('aprovar'), ('desaprovar')) as a(acao)
 where p.nome = 'Admin'
on conflict do nothing;

-- Linha de controle DENTRO da migration: se a renomeação não pegou ninguém,
-- alguma coisa está errada e é melhor estourar aqui do que descobrir pela
-- tela em branco de 24 usuários.
do $$
declare v_restou integer;
begin
  select count(*) into v_restou
    from public.usuario_permissoes where recurso = 'rh.ferias';
  if v_restou > 0 then
    raise exception 'Sobraram % permissões em rh.ferias: a renomeação não fechou', v_restou;
  end if;
end $$;
```

- [ ] **Step 3: Aplicar via MCP e rodar o advisor**

Aplicar com `apply_migration`, `name` = `20260912<hhmmss>_origens_13o_e_recurso`. Depois rodar `get_advisors` (security e performance) e conferir que nada novo apareceu.

- [ ] **Step 4: Atualizar o catálogo do TypeScript**

Em `src/config/recursos.ts`, trocar o bloco de `rh.ferias` (hoje linhas 344-350) por:

```ts
  {
    id: "rh.decimo-terceiro-ferias",
    nome: "13º e Férias",
    modulo: "rh",
    rota: "/rh/decimo-terceiro-e-ferias",
    acoes: CRUD_APROVA,
  },
```

- [ ] **Step 5: Provar que ninguém perdeu acesso**

Rodar via `execute_sql`:

```sql
select
  (select count(*) from perfil_permissoes  where recurso = 'rh.decimo-terceiro-ferias') as perfil,
  (select count(*) from usuario_permissoes where recurso = 'rh.decimo-terceiro-ferias') as usuario,
  (select count(*) from perfil_permissoes  where recurso = 'rh.ferias') as sobrou_perfil,
  (select count(*) from usuario_permissoes where recurso = 'rh.ferias') as sobrou_usuario;
```

Esperado em 12/09/2026, contas fechadas: **`perfil = 11`** (as 9 renomeadas mais `aprovar` e `desaprovar` do Admin) e **`usuario = 32`** (as 24 renomeadas mais 2 ações × 4 usuários do perfil Admin). `sobrou_perfil = 0` e `sobrou_usuario = 0`.

Se os números da Step 1 vierem diferentes de 9 e 24, recalcular: `perfil` = o da Step 1 + 2, e `usuario` = o da Step 1 + (2 × número de usuários com perfil Admin). Número exato, não `>=`: asserção frouxa esconde a linha que não foi renomeada.

**Decisão registrada:** só o Admin ganha `aprovar` e `desaprovar`. O perfil **RH** (1 usuário) fica com o CRUD que já tinha e passa a poder gerar e editar o lote, mas não aprovar. Se o Tiago quiser que o RH aprove, é um `insert` de duas linhas depois, não uma mudança de desenho.

- [ ] **Step 5b: Caçar os usos do id antigo (descoberto na execução, 12/09/2026)**

Renomear o recurso faz `"rh.ferias"` deixar de existir, e **quatro arquivos ainda perguntam por ele**. Os dois primeiros são o estrago de verdade: passariam a devolver `false` para todo mundo, escondendo a seção de Férias sem erro nenhum.

```bash
grep -rn '"rh\.ferias"' src --include="*.ts" --include="*.tsx"
```

Esperado: 7 ocorrências em 4 arquivos.

| Arquivo | Ocorrências |
|---|---|
| `src/app/(app)/cadastros/colaboradores/[id]/page.tsx` | 1 |
| `src/app/(app)/rh/alertas/page.tsx` | 1 |
| `src/app/(app)/rh/ferias/page.tsx` | 4 |
| `src/modules/rh/ferias/actions.ts` | 1 |

Trocar as 7 por `"rh.decimo-terceiro-ferias"`.

**Não trocar** o `idTabela="rh.ferias"` de `src/modules/rh/ferias/components/ferias-tabela.tsx:267`: aquilo é a chave da preferência de coluna e filtro que cada usuário salvou, não um id de permissão. Renomear zera a preferência de todo mundo sem motivo.

Os `href="/rh/ferias"` de `painel-alertas.tsx` e `ficha-colaborador.tsx` ficam para a Task 9, quando a rota nova existir. Antes disso apontariam para 404.

Por que o `grep` e não só o `tsc`: aqui o `tsc` pega, porque `temPermissao` recebe `RecursoId` tipado a partir do catálogo. Mas o `grep` é o que mostra o tamanho do estrago de uma vez, e nem todo uso de string de recurso está num lugar tipado.

- [ ] **Step 6: Rodar typecheck e testes**

```bash
npx tsc --noEmit && npx vitest run src/config --environment=node
```

Esperado: zero erro de tipo. Se `recursos.test.ts` tiver asserção com o id antigo, ela quebra aqui: atualizar para o id novo.

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/20260912*_origens_13o_e_recurso.sql src/config/recursos.ts src/config/recursos.test.ts
git commit -m "feat(rh): abre origens de 13º e férias e renomeia o recurso"
```

---

### Task 2: As tabelas do lote

**Files:**
- Create: `supabase/migrations/20260912<hhmmss>_decimo_terceiro_tabelas.sql`

**Interfaces:**
- Produces: `public.rh_decimo_terceiro` e `public.rh_decimo_terceiro_itens` com as colunas listadas abaixo. As RPCs das Tasks 3 a 6 escrevem nelas; as queries da Task 7 leem.

- [ ] **Step 1: Escrever a migration**

```sql
create table if not exists public.rh_decimo_terceiro (
  id uuid primary key default gen_random_uuid(),
  ano smallint not null,
  parcela smallint not null,
  percentual numeric(7,4) not null,
  com_desconto boolean not null default false,
  status text not null default 'rascunho',
  data_vencimento date,
  valor_bruto numeric(14,2) not null default 0,
  valor_descontos numeric(14,2) not null default 0,
  valor_liquido numeric(14,2) not null default 0,
  aprovado_por uuid references public.usuarios(id),
  aprovado_em timestamptz,
  motivo_rejeicao text,
  excluido_em timestamptz,
  excluido_por uuid references public.usuarios(id),
  motivo_exclusao text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  created_by uuid references public.usuarios(id),
  constraint rh_dt_parcela_check check (parcela in (1, 2)),
  constraint rh_dt_ano_check check (ano between 2000 and 2100),
  -- Teto de 100%: percentual é fração, não porcentagem digitada.
  -- 1.5 aqui significaria 150% do 13º devido.
  constraint rh_dt_percentual_check check (percentual > 0 and percentual <= 1),
  constraint rh_dt_status_check check (status in
    ('rascunho','pendente_aprovacao','aprovado','rejeitado'))
);

-- Índice único PARCIAL, não constraint: o lote excluído libera o par
-- (ano, parcela) para ser gerado de novo.
create unique index if not exists rh_dt_ano_parcela_unico
  on public.rh_decimo_terceiro (ano, parcela) where excluido_em is null;

create table if not exists public.rh_decimo_terceiro_itens (
  id uuid primary key default gen_random_uuid(),
  decimo_terceiro_id uuid not null
    references public.rh_decimo_terceiro(id) on delete cascade,
  colaborador_id uuid not null references public.colaboradores(id),
  centro_custo_id uuid references public.centros_custo(id),
  salario_base numeric(14,2) not null,
  avos smallint not null,
  valor_bruto numeric(14,2) not null default 0,
  valor_ja_pago numeric(14,2) not null default 0,
  valor_inss numeric(14,2) not null default 0,
  valor_irrf numeric(14,2) not null default 0,
  valor_liquido numeric(14,2) not null default 0,
  editado_manualmente boolean not null default false,
  lancamento_id uuid references public.lancamentos(id),
  created_at timestamptz not null default now(),
  constraint rh_dt_itens_avos_check check (avos between 0 and 12),
  constraint rh_dt_itens_salario_check check (salario_base >= 0)
);

create unique index if not exists rh_dt_itens_lote_colaborador_unico
  on public.rh_decimo_terceiro_itens (decimo_terceiro_id, colaborador_id);

create index if not exists rh_dt_itens_lote_idx
  on public.rh_decimo_terceiro_itens (decimo_terceiro_id);

alter table public.rh_decimo_terceiro enable row level security;
alter table public.rh_decimo_terceiro_itens enable row level security;

create policy "rh_dt_select" on public.rh_decimo_terceiro
  for select to authenticated using (true);

create policy "rh_dt_itens_select" on public.rh_decimo_terceiro_itens
  for select to authenticated using (true);

-- Grants EXPLÍCITOS (regra de ouro 1 do CLAUDE.md). Tabela nova não herda
-- privilégio nenhum, e o grant declara SÓ o que as policies permitem: há
-- policy de SELECT e mais nada, logo grant de SELECT e mais nada. Toda
-- escrita passa por RPC security definer.
--
-- O `revoke` vem ANTES do grant e não é decoração: PUBLIC nasce com
-- privilégio em alguns caminhos, e grant sem revoke não fecha coisa nenhuma.
revoke all on public.rh_decimo_terceiro from public, anon, authenticated;
revoke all on public.rh_decimo_terceiro_itens from public, anon, authenticated;

grant select on public.rh_decimo_terceiro to authenticated;
grant select on public.rh_decimo_terceiro_itens to authenticated;

create trigger rh_dt_audit
  after insert or update or delete on public.rh_decimo_terceiro
  for each row execute function public.fn_audit();

create trigger rh_dt_itens_audit
  after insert or update or delete on public.rh_decimo_terceiro_itens
  for each row execute function public.fn_audit();
```

- [ ] **Step 2: Aplicar via MCP e rodar o advisor**

`apply_migration` com `name` = `20260912<hhmmss>_decimo_terceiro_tabelas`. Depois `get_advisors`.

O advisor vai apontar RLS habilitada sem policy de escrita. Isso é **esperado e correto**: toda escrita passa por RPC `security definer`, como na rescisão. Não adicionar policy de insert/update.

- [ ] **Step 3: Conferir que as tabelas nasceram como o esperado**

```sql
select table_name, count(*) as colunas
from information_schema.columns
where table_schema='public'
  and table_name in ('rh_decimo_terceiro','rh_decimo_terceiro_itens')
group by table_name;
```

Esperado: `rh_decimo_terceiro` = 19 colunas, `rh_decimo_terceiro_itens` = 14 colunas.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260912*_decimo_terceiro_tabelas.sql
git commit -m "feat(rh): tabelas do lote de 13º"
```

---

### Task 3: `fn_gerar_decimo_terceiro`, que é onde o dinheiro é calculado

Esta é a task central. Tudo que decide valor está aqui e em lugar nenhum mais.

**Files:**
- Create: `supabase/migrations/20260912<hhmmss>_decimo_terceiro_gerar.sql`
- Create: `supabase/provas/decimo_terceiro.sql`

**Interfaces:**
- Consumes: `fn_rescisao_avos_13(date, date) returns integer` (já existe, migration `20260829210000`), `fn_folha_inss(numeric) `, `fn_folha_irrf(numeric, integer)` (conferir a assinatura viva antes de chamar, ver Step 1).
- Produces: `fn_gerar_decimo_terceiro(p_ano smallint, p_parcela smallint, p_percentual numeric, p_com_desconto boolean, p_data_vencimento date default null) returns uuid` (o id do lote criado).

- [ ] **Step 1: Ler a assinatura viva do que vai ser chamado**

O arquivo do repo mente. Rodar via `execute_sql`:

```sql
select p.proname, pg_get_function_identity_arguments(p.oid) as args,
       pg_get_function_result(p.oid) as retorna
from pg_proc p join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public'
  and p.proname in ('fn_rescisao_avos_13','fn_folha_inss','fn_folha_irrf',
                    'fn_exigir_competencia_aberta','tem_permissao');
```

Anotar as assinaturas exatas. Se `fn_folha_irrf` não receber o número de dependentes do jeito esperado, ajustar a chamada da Step 2 ao que o banco realmente expõe, e registrar o ajuste no comentário da função.

- [ ] **Step 2: Escrever a migration**

```sql
create or replace function public.fn_gerar_decimo_terceiro(
  p_ano smallint,
  p_parcela smallint,
  p_percentual numeric,
  p_com_desconto boolean,
  p_data_vencimento date default null
)
returns uuid
language plpgsql
security definer
set search_path to ''
as $$
declare
  v_uid uuid := (select auth.uid());
  v_lote uuid;
  v_fim date := make_date(p_ano, 12, 31);
  v_col record;
  v_avos smallint;
  v_bruto numeric(14,2);
  v_ja_pago numeric(14,2);
  v_base numeric(14,2);
  v_inss numeric(14,2);
  v_irrf numeric(14,2);
  v_liquido numeric(14,2);
  v_dependentes integer;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'criar') then
    raise exception 'Sem permissao para gerar o 13o';
  end if;

  -- TRAVA 1: desconto ligado com faixa vazia devolve imposto ZERO e o lote
  -- fecha "certo" sem imposto nenhum. Recusa nomeando a tabela vazia.
  if p_com_desconto then
    if not exists (select 1 from public.folha_inss_faixas) then
      raise exception 'Nao ha faixas de INSS cadastradas. Cadastre em /rh/parametros-folha antes de gerar uma parcela com desconto.';
    end if;
    if not exists (select 1 from public.folha_irrf_faixas) then
      raise exception 'Nao ha faixas de IRRF cadastradas. Cadastre em /rh/parametros-folha antes de gerar uma parcela com desconto.';
    end if;
  end if;

  if exists (select 1 from public.rh_decimo_terceiro
              where ano = p_ano and parcela = p_parcela and excluido_em is null) then
    raise exception 'Ja existe um lote de 13o da %a parcela de %.', p_parcela, p_ano;
  end if;

  insert into public.rh_decimo_terceiro
    (ano, parcela, percentual, com_desconto, data_vencimento, created_by)
  values (p_ano, p_parcela, p_percentual, p_com_desconto, p_data_vencimento, v_uid)
  returning id into v_lote;

  -- TRAVA 2: só CLT ativo COM data de admissão. Sem admissão,
  -- fn_rescisao_avos_13 devolve 0 e a pessoa receberia R$ 0,00 calado.
  -- Quem fica de fora é listado pela query da tela, não aqui.
  for v_col in
    select c.id, c.nome, c.salario, c.centro_custo_id, c.data_admissao,
           (select count(*) from public.rh_dependentes d
             where d.colaborador_id = c.id)::integer as dependentes
      from public.colaboradores c
     where c.ativo
       and c.vinculo = 'clt'
       and c.data_admissao is not null
     order by c.nome
  loop
    v_avos := public.fn_rescisao_avos_13(v_col.data_admissao, v_fim);

    -- Dividir ANTES de multiplicar, e o round envolve a divisão.
    -- salario * avos / 12 e round(salario/12,2) * avos divergem em centavos,
    -- e a divergência aparece na conferência do lote inteiro.
    v_bruto := round(coalesce(v_col.salario, 0) / 12, 2) * v_avos * p_percentual;
    v_bruto := round(v_bruto, 2);

    -- Abate o BRUTO da 1ª parcela, não o líquido: o imposto é do 13º inteiro
    -- e é cobrado uma vez só, na parcela que tem a chave ligada. Abater
    -- líquido cobraria imposto sobre imposto.
    v_ja_pago := 0;
    if p_parcela = 2 then
      select coalesce(sum(i.valor_bruto), 0) into v_ja_pago
        from public.rh_decimo_terceiro_itens i
        join public.rh_decimo_terceiro l on l.id = i.decimo_terceiro_id
       where l.ano = p_ano and l.parcela = 1 and l.excluido_em is null
         and l.status = 'aprovado'
         and i.colaborador_id = v_col.id;
    end if;

    v_base := v_bruto - v_ja_pago;

    v_inss := 0;
    v_irrf := 0;
    if p_com_desconto and v_base > 0 then
      -- Tributação EXCLUSIVA: a base é o 13º INTEIRO do ano, não o que sobrou
      -- desta parcela e não o salário do mês somado ao 13º.
      v_inss := public.fn_folha_inss(v_bruto + v_ja_pago);
      v_dependentes := v_col.dependentes;
      v_irrf := public.fn_folha_irrf(v_bruto + v_ja_pago - v_inss, v_dependentes);
    end if;

    v_liquido := v_base - v_inss - v_irrf;

    insert into public.rh_decimo_terceiro_itens
      (decimo_terceiro_id, colaborador_id, centro_custo_id, salario_base,
       avos, valor_bruto, valor_ja_pago, valor_inss, valor_irrf, valor_liquido)
    values
      (v_lote, v_col.id, v_col.centro_custo_id, coalesce(v_col.salario, 0),
       v_avos, v_bruto, v_ja_pago, v_inss, v_irrf, v_liquido);
  end loop;

  perform public.fn_dt_recalcular_totais(v_lote);
  return v_lote;
end $$;

comment on function public.fn_gerar_decimo_terceiro(smallint, smallint, numeric, boolean, date) is
  'Gera o lote de 13o. Dinheiro: round(salario/12,2) * avos * percentual, menos o BRUTO ja pago na 1a parcela, menos INSS/IRRF calculados sobre o 13o INTEIRO quando p_com_desconto. Recusa com faixa fiscal vazia. CLT ativo sem data_admissao NAO entra.';

create or replace function public.fn_dt_recalcular_totais(p_lote uuid)
returns void
language plpgsql
security definer
set search_path to ''
as $$
begin
  update public.rh_decimo_terceiro l
     set valor_bruto = t.bruto,
         valor_descontos = t.descontos,
         valor_liquido = t.liquido,
         updated_at = now()
    from (
      select coalesce(sum(valor_bruto - valor_ja_pago), 0) as bruto,
             coalesce(sum(valor_inss + valor_irrf), 0) as descontos,
             coalesce(sum(valor_liquido), 0) as liquido
        from public.rh_decimo_terceiro_itens
       where decimo_terceiro_id = p_lote
    ) t
   where l.id = p_lote;
end $$;
```

- [ ] **Step 3: Aplicar e conferir que a função EXISTE e tem o corpo esperado**

`apply_migration`, depois:

```sql
select pg_get_functiondef(p.oid) like '%fn_rescisao_avos_13%' as chama_avos,
       pg_get_functiondef(p.oid) like '%folha_inss_faixas%'   as tem_trava
from pg_proc p join pg_namespace n on n.oid=p.pronamespace
where n.nspname='public' and p.proname='fn_gerar_decimo_terceiro';
```

Esperado: as duas colunas `true`. Isso só prova que o texto está lá; a Step 4 é que prova que roda.

- [ ] **Step 4: Escrever a prova, com os números feitos à mão ANTES**

Criar `supabase/provas/decimo_terceiro.sql`. O cabeçalho traz a conta à mão; o corpo confere a RPC contra ela. Molde: `supabase/provas/rescisao.sql`.

```sql
-- Prova do lote de 13º (Bloco 8c).
--
-- A prova CHAMA a RPC. Não basta o apply_migration voltar `success`: plpgsql
-- só valida as queries do corpo na PRIMEIRA EXECUÇÃO. A prova da rescisão
-- estourou exatamente assim, com folha_parametros vazia, depois de a migration
-- ter voltado success e o advisor estar limpo.
--
-- Cobaias (criadas e desfeitas aqui dentro):
--   ZE PROVA 1  salário 3.000,00  admitido 10/01/2026 -> 12 avos
--   ZE PROVA 2  salário 2.400,00  admitido 20/08/2026 -> 4 avos (ago tem 12 dias, NÃO conta)
--   ZE PROVA 3  salário 1.800,00  SEM data de admissão -> não pode entrar no lote
--
-- Conta à mão, 1ª parcela a 50%, SEM desconto:
--   Zé 1: round(3000/12,2)=250,00 x 12 x 0,5            = 1.500,00
--   Zé 2: ago/2026 começa 20/08 -> 12 dias -> não conta.
--         set,out,nov,dez = 4 avos
--         round(2400/12,2)=200,00 x 4 x 0,5             =   400,00
--   Zé 3: fora do lote
--   TOTAL BRUTO DO LOTE                                 = 1.900,00
--   TOTAL LÍQUIDO (sem desconto)                        = 1.900,00
--
-- Conta à mão, 2ª parcela a 100%, SEM desconto:
--   Zé 1: 250,00 x 12 x 1,0 = 3.000,00 menos 1.500,00 já pago = 1.500,00
--   Zé 2:  200,00 x 4 x 1,0 =   800,00 menos   400,00 já pago =   400,00
--   TOTAL LÍQUIDO DA 2ª                                       = 1.900,00

do $$
declare
  v_c1 uuid; v_c2 uuid; v_c3 uuid;
  v_lote1 uuid; v_lote2 uuid;
  v_cc uuid;
  v_bruto numeric; v_liquido numeric; v_itens integer;
  v_z1 numeric; v_z2 numeric;
begin
  select id into v_cc from public.centros_custo limit 1;

  insert into public.colaboradores (nome, vinculo, ativo, salario, data_admissao, centro_custo_id)
  values ('ZE PROVA 1', 'clt', true, 3000.00, date '2026-01-10', v_cc) returning id into v_c1;
  insert into public.colaboradores (nome, vinculo, ativo, salario, data_admissao, centro_custo_id)
  values ('ZE PROVA 2', 'clt', true, 2400.00, date '2026-08-20', v_cc) returning id into v_c2;
  insert into public.colaboradores (nome, vinculo, ativo, salario, data_admissao, centro_custo_id)
  values ('ZE PROVA 3', 'clt', true, 1800.00, null, v_cc) returning id into v_c3;

  -- ===== 1ª parcela, 50%, sem desconto =====
  v_lote1 := public.fn_gerar_decimo_terceiro(2026::smallint, 1::smallint, 0.5, false, null);

  select valor_bruto, valor_liquido into v_bruto, v_liquido
    from public.rh_decimo_terceiro where id = v_lote1;

  select valor_liquido into v_z1 from public.rh_decimo_terceiro_itens
   where decimo_terceiro_id = v_lote1 and colaborador_id = v_c1;
  select valor_liquido into v_z2 from public.rh_decimo_terceiro_itens
   where decimo_terceiro_id = v_lote1 and colaborador_id = v_c2;

  if v_z1 <> 1500.00 then raise exception 'FALHOU Zé 1 na 1a: esperado 1500.00, veio %', v_z1; end if;
  if v_z2 <>  400.00 then raise exception 'FALHOU Zé 2 na 1a: esperado 400.00, veio %',  v_z2; end if;

  -- TRAVA 2: o sem-admissão não pode ter virado item.
  if exists (select 1 from public.rh_decimo_terceiro_itens
              where decimo_terceiro_id = v_lote1 and colaborador_id = v_c3) then
    raise exception 'FALHOU: colaborador sem data_admissao entrou no lote';
  end if;

  -- ===== 2ª parcela, 100%, abatendo a 1ª =====
  update public.rh_decimo_terceiro set status = 'aprovado' where id = v_lote1;
  v_lote2 := public.fn_gerar_decimo_terceiro(2026::smallint, 2::smallint, 1.0, false, null);

  select valor_liquido into v_z1 from public.rh_decimo_terceiro_itens
   where decimo_terceiro_id = v_lote2 and colaborador_id = v_c1;
  if v_z1 <> 1500.00 then raise exception 'FALHOU o abatimento da 2a: esperado 1500.00, veio %', v_z1; end if;

  -- ===== TRAVA 1: desconto ligado com faixa vazia tem que RECUSAR =====
  begin
    perform public.fn_gerar_decimo_terceiro(2025::smallint, 1::smallint, 0.5, true, null);
    raise exception 'FALHOU: gerou com desconto e faixa de INSS vazia';
  exception when others then
    if sqlerrm like 'FALHOU%' then raise; end if;
    -- recusou, que é o esperado
  end;

  -- ===== LINHA DE CONTROLE: um caso que DEVE falhar e falha =====
  -- Se esta não estourar, a prova não está olhando para nada.
  if 1500.00 = 1499.99 then
    raise exception 'impossível';
  end if;

  raise exception 'PROVA OK, desfazendo tudo (lote1=%, lote2=%)', v_lote1, v_lote2;
end $$;
```

- [ ] **Step 5: Rodar a prova e ler a mensagem**

Rodar o conteúdo do arquivo via `execute_sql`.

Esperado: erro `PROVA OK, desfazendo tudo (lote1=..., lote2=...)`. Qualquer mensagem começando com `FALHOU` é falha real e tem que ser consertada antes de seguir.

Conferir que NADA sobrou:

```sql
select count(*) as lotes from public.rh_decimo_terceiro;
select count(*) as zes from public.colaboradores where nome like 'ZE PROVA%';
```

Esperado: `0` nos dois.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260912*_decimo_terceiro_gerar.sql supabase/provas/decimo_terceiro.sql
git commit -m "feat(rh): fn_gerar_decimo_terceiro e a prova do cálculo"
```

---

### Task 4: Ciclo de vida do lote (editar item, enviar, rejeitar, excluir)

**Files:**
- Create: `supabase/migrations/20260912<hhmmss>_decimo_terceiro_ciclo.sql`
- Modify: `supabase/provas/decimo_terceiro.sql` (acrescenta a parte 2)

**Interfaces:**
- Consumes: `fn_dt_recalcular_totais(uuid)` da Task 3.
- Produces: `fn_editar_item_decimo_terceiro(p_item uuid, p_valor numeric)`, `fn_enviar_decimo_terceiro_aprovacao(p_lote uuid)`, `fn_rejeitar_decimo_terceiro(p_lote uuid, p_motivo text)`, `fn_excluir_decimo_terceiro(p_lote uuid, p_motivo text)`, todas `returns void`.

- [ ] **Step 1: Escrever a migration**

Molde exato: `supabase/migrations/20260829210000_rescisoes.sql` linhas 565-760 (`fn_editar_item_rescisao`, `fn_enviar_rescisao_aprovacao`, `fn_rejeitar_rescisao`). As quatro funções seguem o mesmo esqueleto:

```sql
create or replace function public.fn_editar_item_decimo_terceiro(
  p_item uuid, p_valor numeric
)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare
  v_lote uuid;
  v_status text;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'editar') then
    raise exception 'Sem permissao para editar o 13o';
  end if;

  if p_valor < 0 then
    raise exception 'O valor nao pode ser negativo';
  end if;

  select i.decimo_terceiro_id, l.status into v_lote, v_status
    from public.rh_decimo_terceiro_itens i
    join public.rh_decimo_terceiro l on l.id = i.decimo_terceiro_id
   where i.id = p_item and l.excluido_em is null
   for update;

  -- `found`, não `v_lote is null`: ler campo de record depois de um
  -- SELECT INTO sem linha é caminho para erro de runtime, e o erro não diria
  -- que o item simplesmente não existe.
  if not found then raise exception 'Item nao encontrado'; end if;

  if v_status <> 'rascunho' then
    raise exception 'O lote esta em "%": so da para editar em rascunho.', v_status;
  end if;

  update public.rh_decimo_terceiro_itens
     set valor_liquido = p_valor, editado_manualmente = true
   where id = p_item;

  perform public.fn_dt_recalcular_totais(v_lote);
end $$;

create or replace function public.fn_enviar_decimo_terceiro_aprovacao(p_lote uuid)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare v_status text; v_itens integer;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'editar') then
    raise exception 'Sem permissao para enviar o 13o para aprovacao';
  end if;

  select status into v_status from public.rh_decimo_terceiro
   where id = p_lote and excluido_em is null for update;
  if not found then raise exception 'Lote nao encontrado'; end if;

  if v_status <> 'rascunho' then
    raise exception 'O lote esta em "%": so da para enviar o que esta em rascunho.', v_status;
  end if;

  select count(*) into v_itens from public.rh_decimo_terceiro_itens
   where decimo_terceiro_id = p_lote;
  if v_itens = 0 then
    raise exception 'O lote esta vazio: nenhum CLT ativo com data de admissao.';
  end if;

  update public.rh_decimo_terceiro
     set status = 'pendente_aprovacao', motivo_rejeicao = null, updated_at = now()
   where id = p_lote;
end $$;

create or replace function public.fn_rejeitar_decimo_terceiro(p_lote uuid, p_motivo text)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare v_status text;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'aprovar') then
    raise exception 'Sem permissao para rejeitar o 13o';
  end if;

  if btrim(coalesce(p_motivo, ''), E' \t\r\n') = '' then
    raise exception 'Informe o motivo da rejeicao';
  end if;

  select status into v_status from public.rh_decimo_terceiro
   where id = p_lote and excluido_em is null for update;
  if not found then raise exception 'Lote nao encontrado'; end if;

  if v_status <> 'pendente_aprovacao' then
    raise exception 'O lote esta em "%": so da para rejeitar o que esta pendente.', v_status;
  end if;

  update public.rh_decimo_terceiro
     set status = 'rejeitado', motivo_rejeicao = p_motivo, updated_at = now()
   where id = p_lote;
end $$;

create or replace function public.fn_excluir_decimo_terceiro(p_lote uuid, p_motivo text)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare v_status text;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'excluir') then
    raise exception 'Sem permissao para excluir o 13o';
  end if;

  if btrim(coalesce(p_motivo, ''), E' \t\r\n') = '' then
    raise exception 'Informe o motivo da exclusao';
  end if;

  select status into v_status from public.rh_decimo_terceiro
   where id = p_lote and excluido_em is null for update;
  if not found then raise exception 'Lote nao encontrado'; end if;

  -- Lote aprovado tem lançamento no financeiro. Desaprovar primeiro.
  if v_status = 'aprovado' then
    raise exception 'O lote esta aprovado e tem contas a pagar. Desaprove antes de excluir.';
  end if;

  update public.rh_decimo_terceiro
     set excluido_em = now(), excluido_por = (select auth.uid()),
         motivo_exclusao = p_motivo, updated_at = now()
   where id = p_lote;
end $$;
```

- [ ] **Step 2: Aplicar via MCP e rodar o advisor**

- [ ] **Step 3: Acrescentar a parte 2 da prova**

Dentro do mesmo `do $$` de `supabase/provas/decimo_terceiro.sql`, ANTES do `raise exception 'PROVA OK'`, inserir:

```sql
  -- ===== Parte 2: o ciclo de vida =====
  -- Editar item em rascunho muda o total do lote.
  declare v_item uuid; v_antes numeric; v_depois numeric;
  begin
    select id into v_item from public.rh_decimo_terceiro_itens
     where decimo_terceiro_id = v_lote2 and colaborador_id = v_c1;
    select valor_liquido into v_antes from public.rh_decimo_terceiro where id = v_lote2;

    perform public.fn_editar_item_decimo_terceiro(v_item, 1000.00);

    select valor_liquido into v_depois from public.rh_decimo_terceiro where id = v_lote2;
    if v_depois = v_antes then
      raise exception 'FALHOU: editar o item nao mexeu no total do lote';
    end if;
    if v_depois <> v_antes - 500.00 then
      raise exception 'FALHOU: total esperado %, veio %', v_antes - 500.00, v_depois;
    end if;
  end;

  -- Enviar para aprovação e tentar editar: tem que recusar.
  perform public.fn_enviar_decimo_terceiro_aprovacao(v_lote2);
  declare v_item2 uuid;
  begin
    select id into v_item2 from public.rh_decimo_terceiro_itens
     where decimo_terceiro_id = v_lote2 limit 1;
    perform public.fn_editar_item_decimo_terceiro(v_item2, 1.00);
    raise exception 'FALHOU: editou item de lote pendente de aprovacao';
  exception when others then
    if sqlerrm like 'FALHOU%' then raise; end if;
  end;
```

- [ ] **Step 4: Rodar a prova inteira**

Esperado: `PROVA OK, desfazendo tudo`. Se aparecer `FALHOU`, consertar antes de seguir.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260912*_decimo_terceiro_ciclo.sql supabase/provas/decimo_terceiro.sql
git commit -m "feat(rh): ciclo de vida do lote de 13º"
```

---

### Task 5: Aprovar e desaprovar, que é onde o dinheiro sai

**Files:**
- Create: `supabase/migrations/20260912<hhmmss>_decimo_terceiro_aprovar.sql`
- Modify: `supabase/provas/decimo_terceiro.sql` (parte 3)

**Interfaces:**
- Consumes: `fn_exigir_competencia_aberta(p_mes date, p_entidade text, p_id uuid)`.
- Produces: `fn_aprovar_decimo_terceiro(p_lote uuid) returns void`, `fn_desaprovar_decimo_terceiro(p_lote uuid, p_motivo text) returns void`.

- [ ] **Step 1: Reler o molde vivo da aprovação da rescisão**

```sql
select pg_get_functiondef(p.oid)
from pg_proc p join pg_namespace n on n.oid=p.pronamespace
where n.nspname='public' and p.proname='fn_aprovar_rescisao';
```

O bloco de insert em `lancamentos` + `lancamento_parcelas` + `parcela_eventos` + `lancamento_rateios` é copiado dali, trocando `origem` por `decimo_terceiro` e iterando os itens. Copiar da definição VIVA, não do arquivo do repo.

- [ ] **Step 2: Escrever a migration**

```sql
create or replace function public.fn_aprovar_decimo_terceiro(p_lote uuid)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare
  v_l record; v_item record;
  v_uid uuid := (select auth.uid());
  v_comp date; v_venc date; v_lanc uuid; v_parcela uuid;
  v_aprova_pgto boolean := public.tem_permissao('financeiro.aprovacao-pagamentos', 'aprovar');
  v_st_parcela text;
  v_grupo_inss text; v_grupo_irrf text;
  v_tot_inss numeric(14,2); v_tot_irrf numeric(14,2);
  v_venc_guia date;
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'aprovar') then
    raise exception 'Sem permissao para aprovar o 13o';
  end if;

  select * into v_l from public.rh_decimo_terceiro
   where id = p_lote and excluido_em is null for update;
  if not found then raise exception 'Lote nao encontrado'; end if;

  if v_l.status <> 'pendente_aprovacao' then
    raise exception 'O lote esta em "%": so da para aprovar o que esta pendente de aprovacao.', v_l.status;
  end if;

  -- Competência: dezembro do ano do lote. A trava é a mesma da rescisão.
  v_comp := make_date(v_l.ano, 12, 1);
  perform public.fn_exigir_competencia_aberta(v_comp, 'decimo_terceiro', p_lote);

  v_venc := coalesce(v_l.data_vencimento, make_date(v_l.ano, 12, 20));
  v_st_parcela := case when v_aprova_pgto then 'aprovado' else 'pendente' end;

  -- ===== 1. Uma conta a pagar POR PESSOA =====
  -- Por pessoa e não pelo total, porque cada um tem seu centro de custo e o
  -- custo tem que cair na obra certa. fn_aprovar_folha faz assim.
  for v_item in
    select i.*, c.nome
      from public.rh_decimo_terceiro_itens i
      join public.colaboradores c on c.id = i.colaborador_id
     where i.decimo_terceiro_id = p_lote
       and i.valor_liquido > 0     -- líquido zero não gera conta a pagar de R$ 0,00
     order by c.nome
  loop
    insert into public.lancamentos
      (tipo, origem, origem_id, centro_custo_id, descricao, valor, status,
       data_compra, mes_competencia, data_vencimento, created_by)
    values
      ('a_pagar', 'decimo_terceiro', v_item.id, v_item.centro_custo_id,
       '13o ' || v_item.nome || ' ' || v_l.parcela || 'a parcela ' || v_l.ano,
       v_item.valor_liquido, 'a_pagar',
       (now() at time zone 'America/Rio_Branco')::date, v_comp, v_venc, v_uid)
    returning id into v_lanc;

    insert into public.lancamento_parcelas
      (lancamento_id, numero_parcela, valor, data_vencimento, status, created_by,
       aprovado_por, aprovado_em, data_programada, data_programada_origem)
    values (v_lanc, 1, v_item.valor_liquido, v_venc, v_st_parcela, v_uid,
       case when v_aprova_pgto then v_uid end,
       case when v_aprova_pgto then now() end,
       case when v_aprova_pgto then v_venc end,
       case when v_aprova_pgto then 'vencimento' end)
    returning id into v_parcela;

    if v_aprova_pgto then
      insert into public.parcela_eventos (parcela_id, tipo, data_para, created_by)
      values (v_parcela, 'aprovou', v_venc, v_uid);
    end if;

    if v_item.centro_custo_id is not null then
      insert into public.lancamento_rateios (lancamento_id, centro_custo_id, valor, created_by)
      values (v_lanc, v_item.centro_custo_id, v_item.valor_liquido, v_uid);
    end if;

    update public.rh_decimo_terceiro_itens set lancamento_id = v_lanc where id = v_item.id;
  end loop;

  -- ===== 2. As guias do retido =====
  -- Grupo não configurado = retido que NÃO vira conta a pagar e some.
  -- A folha tem o mesmo buraco e a tela avisa antes. Aqui também.
  if v_l.com_desconto then
    select grupo_recolhimento_inss, grupo_recolhimento_irrf, dia_vencimento_guias
      into v_grupo_inss, v_grupo_irrf, v_venc_guia
      from public.folha_parametros where id = 1;

    select coalesce(sum(valor_inss), 0), coalesce(sum(valor_irrf), 0)
      into v_tot_inss, v_tot_irrf
      from public.rh_decimo_terceiro_itens where decimo_terceiro_id = p_lote;

    if v_grupo_inss is not null and v_tot_inss > 0 then
      insert into public.lancamentos
        (tipo, origem, origem_id, centro_custo_id, descricao, valor, status,
         data_compra, mes_competencia, data_vencimento, created_by)
      values ('a_pagar', 'folha_guia', p_lote, null,
        v_grupo_inss || ' 13o ' || v_l.parcela || 'a parcela ' || v_l.ano,
        v_tot_inss, 'a_pagar',
        (now() at time zone 'America/Rio_Branco')::date, v_comp, v_venc, v_uid)
      returning id into v_lanc;
      insert into public.lancamento_parcelas
        (lancamento_id, numero_parcela, valor, data_vencimento, status, created_by)
      values (v_lanc, 1, v_tot_inss, v_venc, 'pendente', v_uid);
    end if;

    if v_grupo_irrf is not null and v_tot_irrf > 0 then
      insert into public.lancamentos
        (tipo, origem, origem_id, centro_custo_id, descricao, valor, status,
         data_compra, mes_competencia, data_vencimento, created_by)
      values ('a_pagar', 'folha_guia', p_lote, null,
        v_grupo_irrf || ' 13o ' || v_l.parcela || 'a parcela ' || v_l.ano,
        v_tot_irrf, 'a_pagar',
        (now() at time zone 'America/Rio_Branco')::date, v_comp, v_venc, v_uid)
      returning id into v_lanc;
      insert into public.lancamento_parcelas
        (lancamento_id, numero_parcela, valor, data_vencimento, status, created_by)
      values (v_lanc, 1, v_tot_irrf, v_venc, 'pendente', v_uid);
    end if;
  end if;

  update public.rh_decimo_terceiro
     set status = 'aprovado', aprovado_por = v_uid, aprovado_em = now(),
         motivo_rejeicao = null, updated_at = now()
   where id = p_lote;
end $$;

create or replace function public.fn_desaprovar_decimo_terceiro(p_lote uuid, p_motivo text)
returns void
language plpgsql
security definer
set search_path to ''
as $$
declare v_l record; v_ids uuid[];
begin
  if not public.tem_permissao('rh.decimo-terceiro-ferias', 'desaprovar') then
    raise exception 'Sem permissao para desaprovar o 13o';
  end if;

  if btrim(coalesce(p_motivo, ''), E' \t\r\n') = '' then
    raise exception 'Informe o motivo da desaprovacao';
  end if;

  select * into v_l from public.rh_decimo_terceiro
   where id = p_lote and excluido_em is null for update;
  if not found then raise exception 'Lote nao encontrado'; end if;
  if v_l.status <> 'aprovado' then
    raise exception 'O lote esta em "%": so da para desaprovar o que esta aprovado.', v_l.status;
  end if;

  -- Parcela já paga trava a volta: o dinheiro saiu do banco.
  if exists (
    select 1 from public.rh_decimo_terceiro_itens i
      join public.lancamento_parcelas lp on lp.lancamento_id = i.lancamento_id
     where i.decimo_terceiro_id = p_lote and lp.status = 'pago'
  ) then
    raise exception 'Ha parcela ja paga neste lote. Nao da para desaprovar.';
  end if;

  select array_agg(lancamento_id) into v_ids
    from public.rh_decimo_terceiro_itens
   where decimo_terceiro_id = p_lote and lancamento_id is not null;

  -- SOLTA a referência ANTES do delete. rh_dt_itens_lancamento_id_fkey é FK
  -- simples, sem on delete set null: inverter a ordem estoura no meio da
  -- desaprovação e deixa metade feito.
  update public.rh_decimo_terceiro_itens
     set lancamento_id = null where decimo_terceiro_id = p_lote;

  if v_ids is not null then
    delete from public.lancamento_rateios   where lancamento_id = any(v_ids);
    delete from public.parcela_eventos      where parcela_id in
      (select id from public.lancamento_parcelas where lancamento_id = any(v_ids));
    delete from public.lancamento_parcelas  where lancamento_id = any(v_ids);
    delete from public.lancamentos          where id = any(v_ids);
  end if;

  -- As guias apontam para o LOTE, não para o item.
  delete from public.lancamento_parcelas where lancamento_id in
    (select id from public.lancamentos where origem = 'folha_guia' and origem_id = p_lote);
  delete from public.lancamentos
   where origem = 'folha_guia' and origem_id = p_lote;

  update public.rh_decimo_terceiro
     set status = 'rascunho', aprovado_por = null, aprovado_em = null,
         motivo_rejeicao = p_motivo, updated_at = now()
   where id = p_lote;
end $$;
```

- [ ] **Step 3: Aplicar via MCP e rodar o advisor**

- [ ] **Step 4: Acrescentar a parte 3 da prova**

Dentro do mesmo `do $$`, antes do `PROVA OK`:

```sql
  -- ===== Parte 3: o dinheiro =====
  declare
    v_lote3 uuid; v_soma_lanc numeric; v_liq numeric; v_qtd integer;
  begin
    v_lote3 := public.fn_gerar_decimo_terceiro(2024::smallint, 1::smallint, 0.5, false, null);
    perform public.fn_enviar_decimo_terceiro_aprovacao(v_lote3);
    perform public.fn_aprovar_decimo_terceiro(v_lote3);

    select valor_liquido into v_liq from public.rh_decimo_terceiro where id = v_lote3;

    select coalesce(sum(l.valor), 0), count(*) into v_soma_lanc, v_qtd
      from public.lancamentos l
     where l.origem = 'decimo_terceiro'
       and l.origem_id in (select id from public.rh_decimo_terceiro_itens
                            where decimo_terceiro_id = v_lote3);

    -- A soma dos lançamentos TEM que bater com o líquido do lote.
    if v_soma_lanc <> v_liq then
      raise exception 'FALHOU: lote liquido % mas lancamentos somam %', v_liq, v_soma_lanc;
    end if;
    if v_qtd = 0 then
      raise exception 'FALHOU: aprovou e nao gerou lancamento nenhum';
    end if;

    -- Desaprovar tem que devolver TUDO.
    perform public.fn_desaprovar_decimo_terceiro(v_lote3, 'prova');
    select count(*) into v_qtd from public.lancamentos
     where origem = 'decimo_terceiro'
       and origem_id in (select id from public.rh_decimo_terceiro_itens
                          where decimo_terceiro_id = v_lote3);
    if v_qtd <> 0 then
      raise exception 'FALHOU: desaprovou e sobraram % lancamentos', v_qtd;
    end if;
  end;
```

- [ ] **Step 5: Rodar a prova inteira e conferir que não sobrou nada**

Esperado: `PROVA OK, desfazendo tudo`. Depois:

```sql
select count(*) from public.lancamentos where origem = 'decimo_terceiro';
```

Esperado: `0`.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260912*_decimo_terceiro_aprovar.sql supabase/provas/decimo_terceiro.sql
git commit -m "feat(rh): aprovar e desaprovar o lote de 13º"
```

---

### Task 6: Schemas e queries do TypeScript

**Files:**
- Create: `src/modules/rh/decimo-terceiro/schemas.ts`
- Create: `src/modules/rh/decimo-terceiro/schemas.test.ts`
- Create: `src/modules/rh/decimo-terceiro/queries.ts`

**Interfaces:**
- Produces: `gerarLoteSchema`, `editarItemSchema`, `motivoSchema`; `listarLotes(): Promise<LoteLista[]>`, `buscarLote(id): Promise<LoteDetalhe | null>`, `listarForaDoLote(ano): Promise<ForaDoLote[]>`, `temProvisaoDe13Ativa(): Promise<boolean>`.

- [ ] **Step 1: Escrever o teste do schema primeiro**

`src/modules/rh/decimo-terceiro/schemas.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { gerarLoteSchema } from "@/modules/rh/decimo-terceiro/schemas";

describe("gerarLoteSchema", () => {
  it("aceita a 1ª parcela a 50%", () => {
    const r = gerarLoteSchema.safeParse({
      ano: 2026, parcela: 1, percentual: "50", comDesconto: false,
    });
    expect(r.success).toBe(true);
    // 50 digitado vira a fração 0,5 que o banco espera.
    if (r.success) expect(r.data.percentual).toBe(0.5);
  });

  it("recusa percentual acima de 100", () => {
    const r = gerarLoteSchema.safeParse({
      ano: 2026, parcela: 1, percentual: "150", comDesconto: false,
    });
    expect(r.success).toBe(false);
  });

  it("recusa percentual zero", () => {
    const r = gerarLoteSchema.safeParse({
      ano: 2026, parcela: 1, percentual: "0", comDesconto: false,
    });
    expect(r.success).toBe(false);
  });

  it("lê vírgula como separador decimal", () => {
    // 1,5% digitado vira 1500% se o parser tratar a vírgula como milhar.
    const r = gerarLoteSchema.safeParse({
      ano: 2026, parcela: 1, percentual: "33,33", comDesconto: false,
    });
    expect(r.success).toBe(true);
    if (r.success) expect(r.data.percentual).toBeCloseTo(0.3333, 4);
  });

  it("recusa parcela fora de 1 e 2", () => {
    const r = gerarLoteSchema.safeParse({
      ano: 2026, parcela: 3, percentual: "50", comDesconto: false,
    });
    expect(r.success).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npx vitest run src/modules/rh/decimo-terceiro/schemas.test.ts --environment=node
```

Esperado: FAIL, `Cannot find module '@/modules/rh/decimo-terceiro/schemas'`.

- [ ] **Step 3: Escrever o schema**

`src/modules/rh/decimo-terceiro/schemas.ts`. Usar `paraNumero` de `@/modules/rh/percentual`, que já trata a vírgula. **Não** usar o de `parametros-folha/schemas.ts`: aquele tem o furo do ponto como separador de milhar, documentado de propósito.

```ts
import { z } from "zod";

import { idSchemaCom } from "@/lib/id";
import { paraNumero } from "@/modules/rh/percentual";

/** Percentual digitado (50, 33,33) vira a fração que o banco espera (0.5). */
const percentualSchema = z
  .union([z.string(), z.number()])
  .transform((valor, ctx) => {
    const numero = typeof valor === "number" ? valor : paraNumero(valor);
    if (numero === null || Number.isNaN(numero)) {
      ctx.addIssue({ code: "custom", message: "Informe o percentual" });
      return z.NEVER;
    }
    if (numero <= 0 || numero > 100) {
      ctx.addIssue({ code: "custom", message: "O percentual tem que estar entre 0 e 100" });
      return z.NEVER;
    }
    return numero / 100;
  });

export const gerarLoteSchema = z.object({
  ano: z.number().int().min(2000).max(2100),
  parcela: z.union([z.literal(1), z.literal(2)]),
  percentual: percentualSchema,
  comDesconto: z.boolean(),
  dataVencimento: z
    .union([z.string(), z.null()])
    .transform((v) => (v === null || v.trim() === "" ? null : v.trim()))
    .refine((v) => v === null || /^\d{4}-\d{2}-\d{2}$/.test(v), {
      error: "Data inválida",
    })
    .optional(),
});

export type GerarLoteInput = z.input<typeof gerarLoteSchema>;

export const editarItemSchema = z.object({
  itemId: idSchemaCom("Item inválido"),
  valor: z
    .union([z.string(), z.number()])
    .transform((valor, ctx) => {
      const numero = typeof valor === "number" ? valor : paraNumero(valor);
      if (numero === null || Number.isNaN(numero) || numero < 0) {
        ctx.addIssue({ code: "custom", message: "Valor inválido" });
        return z.NEVER;
      }
      return numero;
    }),
});

export const motivoSchema = z.object({
  loteId: idSchemaCom("Lote inválido"),
  motivo: z.string().trim().min(1, { error: "Informe o motivo" }).max(500),
});
```

- [ ] **Step 4: Rodar e ver passar**

```bash
npx vitest run src/modules/rh/decimo-terceiro/schemas.test.ts --environment=node
```

Esperado: 5 passed.

- [ ] **Step 5: Quebrar de propósito e confirmar que o teste cai**

Trocar `numero / 100` por `numero` no `percentualSchema` e rodar de novo. Esperado: FAIL nos testes de 50% e de vírgula. Desfazer a quebra.

Teste que sobrevive à mutação não está provando nada. Se algum teste continuar verde com a quebra, ele está errado.

- [ ] **Step 6: Escrever as queries**

`src/modules/rh/decimo-terceiro/queries.ts`, com `import "server-only"` na primeira linha. Molde: `src/modules/rh/rescisoes/queries.ts`.

Quatro funções:

```ts
/** Lote na listagem. */
export interface LoteLista {
  id: string; ano: number; parcela: number; percentual: number;
  comDesconto: boolean; status: string;
  valorBruto: number; valorDescontos: number; valorLiquido: number;
  quantidadePessoas: number;
}

/** Detalhe do lote, com os itens. */
export interface LoteDetalhe extends LoteLista {
  dataVencimento: string | null;
  motivoRejeicao: string | null;
  itens: ItemDoLote[];
}

export interface ItemDoLote {
  id: string; colaboradorId: string; colaboradorNome: string;
  centroCustoId: string | null; centroCustoNome: string | null;
  salarioBase: number; avos: number;
  valorBruto: number; valorJaPago: number;
  valorInss: number; valorIrrf: number; valorLiquido: number;
  editadoManualmente: boolean; lancamentoId: string | null;
}

/** CLT ativo que NÃO entra no lote, com o motivo. */
export interface ForaDoLote {
  colaboradorId: string; colaboradorNome: string; motivo: string;
}
```

`listarForaDoLote(ano)` busca `colaboradores` com `ativo = true and vinculo = 'clt' and data_admissao is null` e devolve cada um com `motivo: "Sem data de admissão no cadastro"`. É a fonte do bloco da tela.

`temProvisaoDe13Ativa()` devolve `true` quando existe linha em `folha_provisoes` ativa cujo `nome` casa com 13º. É o que liga o aviso de custo em dobro.

- [ ] **Step 7: Typecheck e commit**

```bash
npx tsc --noEmit
git add src/modules/rh/decimo-terceiro/
git commit -m "feat(rh): schemas e queries do 13º"
```

---

### Task 7: Agregação para a tela (`calculo.ts`)

**Files:**
- Create: `src/modules/rh/decimo-terceiro/calculo.ts`
- Create: `src/modules/rh/decimo-terceiro/calculo.test.ts`

**Interfaces:**
- Consumes: `LoteDetalhe`, `ItemDoLote` da Task 6.
- Produces: `resumoPorCentroCusto(lote: LoteDetalhe): CustoPorCentro[]`, `conferenciaDoLote(lote: LoteDetalhe): Conferencia`.

**Este arquivo NÃO calcula dinheiro.** Ele soma o que a RPC já gravou, para a tela conferir. A fórmula do 13º vive em `fn_gerar_decimo_terceiro` e em lugar nenhum mais.

- [ ] **Step 1: Escrever o teste primeiro**

```ts
import { describe, expect, it } from "vitest";

import { conferenciaDoLote, resumoPorCentroCusto } from "@/modules/rh/decimo-terceiro/calculo";
import type { LoteDetalhe } from "@/modules/rh/decimo-terceiro/queries";

function item(over: Partial<LoteDetalhe["itens"][number]> = {}) {
  return {
    id: "i1", colaboradorId: "c1", colaboradorNome: "Zé",
    centroCustoId: "cc1", centroCustoNome: "Obra A",
    salarioBase: 3000, avos: 12,
    valorBruto: 1500, valorJaPago: 0,
    valorInss: 0, valorIrrf: 0, valorLiquido: 1500,
    editadoManualmente: false, lancamentoId: null,
    ...over,
  };
}

function lote(itens: LoteDetalhe["itens"]): LoteDetalhe {
  return {
    id: "l1", ano: 2026, parcela: 1, percentual: 0.5, comDesconto: false,
    status: "rascunho", valorBruto: 0, valorDescontos: 0, valorLiquido: 0,
    quantidadePessoas: itens.length, dataVencimento: null,
    motivoRejeicao: null, itens,
  };
}

describe("resumoPorCentroCusto", () => {
  it("soma o líquido por centro e ordena do maior para o menor", () => {
    const resumo = resumoPorCentroCusto(lote([
      item({ id: "a", centroCustoId: "cc1", centroCustoNome: "Obra A", valorLiquido: 100 }),
      item({ id: "b", centroCustoId: "cc2", centroCustoNome: "Obra B", valorLiquido: 300 }),
      item({ id: "c", centroCustoId: "cc1", centroCustoNome: "Obra A", valorLiquido: 50 }),
    ]));

    expect(resumo).toEqual([
      { centroCustoId: "cc2", centroCustoNome: "Obra B", valorLiquido: 300 },
      { centroCustoId: "cc1", centroCustoNome: "Obra A", valorLiquido: 150 },
    ]);
  });

  it("agrupa quem não tem centro num grupo próprio", () => {
    const resumo = resumoPorCentroCusto(lote([
      item({ id: "a", centroCustoId: null, centroCustoNome: null, valorLiquido: 80 }),
    ]));
    expect(resumo[0]?.centroCustoNome).toBe("Sem centro de custo");
  });
});

describe("conferenciaDoLote", () => {
  it("fecha quando a soma dos itens bate com o total gravado", () => {
    const l = lote([item({ valorLiquido: 1500 }), item({ id: "i2", valorLiquido: 400 })]);
    l.valorLiquido = 1900;
    expect(conferenciaDoLote(l).fecha).toBe(true);
    expect(conferenciaDoLote(l).diferenca).toBe(0);
  });

  it("acusa quando o total gravado diverge da soma dos itens", () => {
    const l = lote([item({ valorLiquido: 1500 })]);
    l.valorLiquido = 1400;
    const c = conferenciaDoLote(l);
    expect(c.fecha).toBe(false);
    expect(c.diferenca).toBe(100);
  });

  it("conta quantos itens foram editados à mão", () => {
    const l = lote([item({ editadoManualmente: true }), item({ id: "i2" })]);
    expect(conferenciaDoLote(l).editadosAMao).toBe(1);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npx vitest run src/modules/rh/decimo-terceiro/calculo.test.ts --environment=node
```

Esperado: FAIL, módulo não encontrado.

- [ ] **Step 3: Implementar**

```ts
/**
 * Agregação pura do lote de 13º para a tela. Sem Supabase, e SEM conta de
 * dinheiro: a fórmula do 13º vive em fn_gerar_decimo_terceiro e em lugar
 * nenhum mais. Aqui só se soma o que a RPC já gravou, para a tela poder
 * conferir. Precedente: src/modules/rh/folha/calculo.ts.
 */
import type { LoteDetalhe } from "@/modules/rh/decimo-terceiro/queries";

export interface CustoPorCentro {
  centroCustoId: string | null;
  centroCustoNome: string | null;
  valorLiquido: number;
}

export function resumoPorCentroCusto(lote: LoteDetalhe): CustoPorCentro[] {
  const grupos = new Map<string, CustoPorCentro>();

  for (const item of lote.itens) {
    const chave = item.centroCustoId ?? "__sem_centro__";
    const atual = grupos.get(chave);
    if (atual) {
      atual.valorLiquido += item.valorLiquido;
    } else {
      grupos.set(chave, {
        centroCustoId: item.centroCustoId,
        centroCustoNome: item.centroCustoNome ?? "Sem centro de custo",
        valorLiquido: item.valorLiquido,
      });
    }
  }

  return [...grupos.values()].sort((a, b) => b.valorLiquido - a.valorLiquido);
}

export interface Conferencia {
  somaDosItens: number;
  totalGravado: number;
  diferenca: number;
  fecha: boolean;
  editadosAMao: number;
}

/**
 * A soma dos itens contra o total gravado no lote. Divergência significa que
 * fn_dt_recalcular_totais não rodou depois de alguma edição, e o número que a
 * tela mostra não é o que vai virar conta a pagar.
 */
export function conferenciaDoLote(lote: LoteDetalhe): Conferencia {
  const soma = lote.itens.reduce((total, item) => total + item.valorLiquido, 0);
  // Centavos: comparar float direto acusaria diferença que não existe.
  const diferenca = Math.round((soma - lote.valorLiquido) * 100) / 100;

  return {
    somaDosItens: soma,
    totalGravado: lote.valorLiquido,
    diferenca: Math.abs(diferenca),
    fecha: diferenca === 0,
    editadosAMao: lote.itens.filter((item) => item.editadoManualmente).length,
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Esperado: 5 passed.

- [ ] **Step 5: Quebrar de propósito**

Trocar o `sort` de `b - a` para `a - b` e rodar: o teste de ordenação tem que cair. Desfazer.

- [ ] **Step 6: Commit**

```bash
git add src/modules/rh/decimo-terceiro/calculo.ts src/modules/rh/decimo-terceiro/calculo.test.ts
git commit -m "feat(rh): agregação do lote de 13º para a tela"
```

---

### Task 8: Server actions

**Files:**
- Create: `src/modules/rh/decimo-terceiro/actions.ts`

**Interfaces:**
- Consumes: os schemas da Task 6, as RPCs das Tasks 3 a 5.
- Produces: `gerarLote`, `editarItem`, `enviarParaAprovacao`, `aprovarLote`, `rejeitarLote`, `desaprovarLote`, `excluirLote`, todas devolvendo `ResultadoAcao = { ok: true } | { erro: string }`, e `gerarLote` devolvendo `{ ok: true; id: string } | { erro: string }`.

- [ ] **Step 1: Escrever o arquivo**

Molde exato: `src/modules/rh/rescisoes/actions.ts`. Copiar `mensagemDeNegocio`, `checarPermissao` e `revalidarTelas` dali, trocando `RECURSO` e `ROTA`.

```ts
const RECURSO = "rh.decimo-terceiro-ferias" as const;
const ROTA = "/rh/decimo-terceiro-e-ferias";

function revalidarTelas(): void {
  revalidatePath(ROTA);
  // revalidatePath(ROTA) sozinho NÃO alcança a rota dinâmica do detalhe:
  // precisa do segundo argumento "page".
  revalidatePath(`${ROTA}/13o/[id]`, "page");
}
```

Três pontos que não podem ser esquecidos:

1. **`mensagemDeNegocio` devolve `error.message` só no SQLSTATE `P0001`.** É o que faz o toast dizer "Não há faixas de INSS cadastradas" em vez de "não foi possível". Sem isso as travas ficam mudas.
2. **Nada depois do sucesso pode virar falha.** `revalidatePath` que estoura não pode derrubar um lote já aprovado no banco. Envolver a revalidação em `try/catch` que só loga.
3. **`?? undefined` para omitir parâmetro opcional** e deixar valer o DEFAULT do banco, como em `gerarRescisao`.

```ts
export async function gerarLote(dados: unknown): Promise<ResultadoGeracao> {
  if (!(await checarPermissao("criar"))) {
    return { erro: "Você não tem permissão para gerar o 13º" };
  }

  const validado = gerarLoteSchema.safeParse(dados);
  if (!validado.success) {
    return { erro: validado.error.issues[0]?.message ?? "Dados inválidos" };
  }

  const supabase = await createClient();
  const { data, error } = await supabase.rpc("fn_gerar_decimo_terceiro", {
    p_ano: validado.data.ano,
    p_parcela: validado.data.parcela,
    p_percentual: validado.data.percentual,
    p_com_desconto: validado.data.comDesconto,
    p_data_vencimento: validado.data.dataVencimento ?? undefined,
  });

  if (error || !data) {
    return { erro: mensagemDeNegocio("gerar", error, "Não foi possível gerar o lote") };
  }

  revalidarTelas();
  return { ok: true, id: data };
}
```

As outras seis seguem o mesmo formato, cada uma com a permissão certa: `editarItem` e `enviarParaAprovacao` pedem `editar`; `aprovarLote` e `rejeitarLote` pedem `aprovar`; `desaprovarLote` pede `desaprovar`; `excluirLote` pede `excluir`.

- [ ] **Step 2: Regenerar os tipos do banco**

```bash
npx supabase gen types typescript --project-id vsesgvqjgqpapoxhnbqx > src/lib/database.types.ts
```

Isso APAGA edição manual no arquivo. Conferir com `git diff src/lib/database.types.ts` que só apareceram as tabelas e funções novas; se alguma edição manual sumiu, refazer.

- [ ] **Step 3: Typecheck**

```bash
npx tsc --noEmit
```

Esperado: zero erro. Se a RPC reclamar de argumento, a assinatura do banco diverge do que a action manda: conferir com `pg_get_function_identity_arguments` e acertar a action, não o banco.

- [ ] **Step 4: Commit**

```bash
git add src/modules/rh/decimo-terceiro/actions.ts src/lib/database.types.ts
git commit -m "feat(rh): actions do lote de 13º"
```

---

### Task 9: As telas

**Files:**
- Create: `src/app/(app)/rh/decimo-terceiro-e-ferias/page.tsx`
- Create: `src/app/(app)/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx`
- Create: `src/modules/rh/decimo-terceiro/components/lotes-tabela.tsx`
- Create: `src/modules/rh/decimo-terceiro/components/gerar-lote-drawer.tsx`
- Create: `src/modules/rh/decimo-terceiro/components/lote-detalhe.tsx`
- Create: `src/modules/rh/decimo-terceiro/components/excluidos-do-lote.tsx`
- Create: `src/modules/rh/decimo-terceiro/components/editar-item-drawer.tsx`
- Modify: `src/app/(app)/rh/ferias/page.tsx`

**Interfaces:**
- Consumes: queries da Task 6, `calculo.ts` da Task 7, actions da Task 8.

- [ ] **Step 0: Invocar a skill `frontend-design`**

O `CLAUDE.md` do projeto manda: *"`frontend-design` ao criar qualquer tela nova"*. São cinco componentes e duas páginas novas, então vale para esta task inteira.

E antes de escrever componente: **canônico primeiro** (regra de ouro 9). `DataTable`, `FormDrawer`, `ApprovalBar`, `KPICard`, `FilterBar`, `MoneyText`, `EmptyState`, `StatusBadge` e `Trilha` já existem em `src/components/canonicos/`. Se um não cobrir, evoluir o canônico, nunca duplicar.

Três detalhes do design system que valem aqui:
- Valor monetário sai em `MoneyText` com `tabular-nums`, alinhado à direita.
- `StatusBadge` usa o verde #15803D de "aprovado", que **não** é o verde #3E7744 da marca.
- Não existe barra de abas dentro da página: a navegação do módulo vive no submenu da sidebar, alimentada por `abasVisiveis`. Registrar em `recursos.ts` (feito na Task 1) é o que faz a aba aparecer.

- [ ] **Step 1: A página principal, com as duas seções**

`/rh/decimo-terceiro-e-ferias/page.tsx` renderiza, nesta ordem: a seção de **Férias** (reusando `src/modules/rh/ferias/components/ferias-tabela.tsx` como está hoje, sem a coluna Recibo, que é do plano 2) e a seção de **13º** com `lotes-tabela.tsx`.

Ação de página (**Gerar 13º**) vai no **cabeçalho**, não solta no corpo. Padrão do projeto.

Barra de ação com `flex-wrap` e **sem** `sm:flex-nowrap`: a faixa de 640 a 816px abre buraco.

- [ ] **Step 2: O redirect da rota antiga**

`src/app/(app)/rh/ferias/page.tsx` inteiro vira:

```tsx
import { permanentRedirect } from "next/navigation";

export default function FeriasRotaAntiga(): never {
  permanentRedirect("/rh/decimo-terceiro-e-ferias");
}
```

- [ ] **Step 3: O drawer de gerar**

Campos: ano (número), parcela (1 ou 2), percentual, chave "Descontar INSS e IRRF", vencimento (opcional).

Duas coisas obrigatórias:

1. **A sugestão de percentual muda com a parcela:** 50 na 1ª, 100 na 2ª. 50% na 2ª faria o líquido dar zero, e isso não pode passar por descuido.
2. **`.default("")` NÃO pode aparecer no schema do formulário.** Quebra o RHF porque o tipo de entrada deixa de bater com o de saída. Usar `defaultValues` no `useForm`.

E o aviso de provisão: se `temProvisaoDe13Ativa()` devolver `true`, mostrar em vermelho, dentro do drawer, antes do botão: *"Existe provisão de 13º ativa na folha. Pagar aqui vai contar o custo duas vezes."*

- [ ] **Step 4: A tela de detalhe**

`/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx` mostra: cabeçalho com ano, parcela, percentual, status e totais; a tabela de itens (nome, centro, salário, avos, bruto, já pago, INSS, IRRF, líquido); o bloco `excluidos-do-lote.tsx`; e as ações conforme o status.

O bloco de excluídos aparece **sempre que houver alguém de fora**, mesmo com o lote aprovado, com o texto: *"N colaboradores fora do lote"*, o nome de cada um, o motivo e o link para o cadastro.

`h-full` não pode ser usado nos cards do cabeçalho: desliga o stretch do flex e os cards ficam de alturas diferentes.

- [ ] **Step 4b: O que a renomeação da rota quebra (descoberto na execução, 12/09/2026)**

Trocar a rota deixa três coisas apontando para o lugar errado. A primeira é a que importa:

1. **`src/modules/rh/ferias/actions.ts` revalida `"/rh/ferias"`**, que virou só um redirect. Criar ou editar férias deixaria de revalidar a página de verdade, e a tabela mostraria dado velho sem erro nenhum. Trocar a constante `ROTA`.
2. **Quatro links de navegação** apontam para a rota antiga: 1 em `ficha-colaborador.tsx` e 3 em `painel-alertas.tsx`. O redirect resolve, mas custa uma ida a mais por clique.
3. **`filtros-sessao.test.ts:153`** usa `"/rh/ferias"` como "uma rota qualquer sem filtro guardado". É string arbitrária, **não mexer**.

```bash
grep -rn '"/rh/ferias"' src --include="*.ts" --include="*.tsx"
```

- [ ] **Step 4c: `SecaoDetalhe` sobe para canônicos**

A página tem duas seções e precisa do cabeçalho de seção. O componente existe em `src/modules/compras/_shared/secao-detalhe.tsx`, e **metade dos quatro consumidores já é de fora de Compras** (`ficha-colaborador` em Cadastros, `painel-alertas` no RH). Importar componente de dentro de outro módulo é o caminho da duplicação que a regra 9 do CLAUDE.md proíbe, então a saída certa é promover, não copiar.

`git mv` para `src/components/canonicos/secao-detalhe.tsx`, exportar no `index.ts`, e trocar o import nos quatro. Conferir com `tsc`.

- [ ] **Step 5: Typecheck, lint e build**

```bash
npx tsc --noEmit && npx next lint && npx next build
```

Esperado: os três verdes. O build é o que pega import de servidor vazando para client.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(app\)/rh/ src/modules/rh/decimo-terceiro/components/
git commit -m "feat(rh): telas do 13º e a aba 13º e Férias"
```

---

### Task 10: `maxDuration` e o teste que guarda

**Files:**
- Modify: `src/app/(app)/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx`
- Create: `src/app/(app)/rh/decimo-terceiro-e-ferias/maxduration.test.ts`

- [ ] **Step 1: Escrever o teste primeiro**

A action de aprovar roda na função da PÁGINA, não numa função própria. Com 29 itens ela escreve 29 lançamentos, 29 parcelas, rateios e guias. Sem `maxDuration` o clique toma timeout com o lote meio gravado.

```ts
import { readFileSync } from "node:fs";
import { join } from "node:path";

import { describe, expect, it } from "vitest";

/**
 * A aprovação do lote roda na função da PÁGINA do detalhe. Um lote de 29
 * pessoas escreve 29 lançamentos, 29 parcelas, rateios e guias numa
 * transação. Sem maxDuration a rota morre no teto padrão e o lote fica meio
 * gravado. Este teste é a guarda: quem apagar a linha vê o teste cair.
 */
describe("maxDuration da rota do lote de 13º", () => {
  it("a página do detalhe declara maxDuration de pelo menos 60s", () => {
    const caminho = join(
      process.cwd(),
      "src/app/(app)/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx",
    );
    const fonte = readFileSync(caminho, "utf8");

    const achado = /export\s+const\s+maxDuration\s*=\s*(\d+)/.exec(fonte);
    expect(achado, "a rota não declara maxDuration").not.toBeNull();
    expect(Number(achado?.[1])).toBeGreaterThanOrEqual(60);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npx vitest run src/app/\(app\)/rh/decimo-terceiro-e-ferias/maxduration.test.ts --environment=node
```

Esperado: FAIL, "a rota não declara maxDuration".

- [ ] **Step 3: Declarar na página**

No topo de `src/app/(app)/rh/decimo-terceiro-e-ferias/13o/[id]/page.tsx`:

```tsx
/**
 * A action de aprovar roda nesta função. 29 itens = 29 lançamentos, 29
 * parcelas, rateios e guias numa transação só. O teto padrão não dá conta.
 */
export const maxDuration = 60;
```

- [ ] **Step 4: Rodar e ver passar**

Esperado: 1 passed.

- [ ] **Step 5: Suíte inteira, typecheck, lint e build**

```bash
npx tsc --noEmit && npx next lint && npx vitest run --environment=node && npx next build
```

Os quatro têm que estar verdes. **Exit 0 sem suíte não é suíte verde:** conferir na saída do vitest que a contagem de testes subiu em relação ao início do trabalho, e que nenhum foi pulado.

- [ ] **Step 5b: Rodar `engineering:code-review` antes de abrir o PR**

O `CLAUDE.md` manda: *"`engineering:code-review` antes de cada merge"*. Rodar sobre o diff inteiro da branch e tratar o que aparecer antes de pedir review humano.

Conferir também a definição de pronto do projeto, item por item: `tsc`/lint/build verdes, sem `any` novo, sem `console.log`, RLS testada (usuário sem permissão não vê e não muta, teste e não suposição), aba some do menu sem permissão de ver, botões somem sem a ação, auditoria gravando, estado vazio com ação, e `MoneyText` em todo valor.

- [ ] **Step 6: Commit e PR**

```bash
git add -A && git commit -m "feat(rh): maxDuration na rota de aprovação do 13º"
git push -u origin decimo-terceiro-e-ferias
gh pr create --title "feat(rh): 13º em lote (Bloco 8c)" --body-file /tmp/pr-13o.md
```

Escrever `/tmp/pr-13o.md` com estas cinco seções, nesta ordem:

1. **O que faz:** lote de 13º por ano e parcela, percentual escolhido ao gerar, 2ª parcela abatendo o bruto da 1ª, conta a pagar por colaborador e guia por grupo de recolhimento.
2. **As três travas:** faixa fiscal vazia com desconto ligado recusa; CLT sem `data_admissao` fica de fora e é listado; competência fechada barra a aprovação.
3. **Como foi provado:** `supabase/provas/decimo_terceiro.sql`, números à mão no cabeçalho, RPCs chamadas de verdade, transação desfeita no fim. Colar a saída `PROVA OK`.
4. **Pendências do Tiago:** 18 dos 29 CLT sem data de admissão (ficam de fora do lote até ele preencher); faixas de INSS/IRRF vazias (só bloqueiam a parcela com desconto).
5. **Fora de escopo:** abatimento da provisão (tem aviso na tela), férias (plano 2), abono pecuniário, eSocial.

Terminar o corpo com a linha de atribuição do projeto.

**`--subject` com um token só** se o merge precisar ser feito no modo `!`: sem espaço, sem `()`, `#` nem aspas, senão o `gh` abre editor e trava.

Esperar o CI verde ANTES de mergear. E lembrar: **check verde prova que buildou, não que a tela abre.** O preview dá 500 neste projeto, então a conferência de verdade é na produção depois do merge.

---

## Depois do merge

1. Rodar `supabase/provas/decimo_terceiro.sql` uma vez contra produção e confirmar `PROVA OK`.
2. Avisar o Tiago das duas pendências que são dele: **18 CLT sem data de admissão** e as **faixas de INSS/IRRF vazias** (só bloqueiam a parcela com desconto).
3. O plano 2 (férias, Bloco 8d) reusa `fn_folha_inss`, `fn_folha_irrf`, o caminho do lançamento e a coluna Recibo na tabela de férias.
