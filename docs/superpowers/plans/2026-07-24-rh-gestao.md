# RH — melhorias de gestão — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development, tarefa a tarefa. Passos usam checkbox (`- [ ]`).

**Goal:** Fechar 4 gaps de gestão do RH: salário+diária no cadastro (destrava folha), dados bancários, anexos em ASO/EPI/atestado, e ficha unificada do colaborador.

**Architecture:** Colunas novas + form no cadastro (colaboradores); extensão do mapa de recurso dos anexos + reuso do AnexosRegistro nas telas de RH; nova rota de ficha do colaborador agregando as fontes de RH (read-only). Migrations via MCP no projeto vivo `vsesgvqjgqpapoxhnbqx`, lendo a fn viva antes de alterar.

**Tech Stack:** Next.js 16 (TS strict), Supabase (RLS) via MCP, RHF+Zod, Vitest, canônicos. Branch: `feat-rh-gestao`.

Spec: `docs/superpowers/specs/2026-07-24-rh-gestao-design.md`.

## Global Constraints
- Não inventar regra fiscal/trabalhista. Folha oficial/eSocial/rescisão fora de escopo.
- RLS/grants/auditoria; permissão tripla; dinheiro NUMERIC(14,2). Migrations via MCP + `.sql` versionado; `get_advisors`; ler fn viva antes de alterar; regenerar tipos após DDL.
- Anexos reusam o padrão do #10 (buscar `anexosIniciais` no server, passar como prop — não travar em "Carregando").
- Portão por tarefa: typecheck/lint/build verdes; testes existentes verdes; sem any novo, sem console.log. (Ambiente: limpar `.next` dup antes do typecheck.)
- Canônicos; toda rota nova tem `loading.tsx`. Commit termina com `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## Task 1: Cadastro — remuneração (salário/diária) + dados bancários (#1/#2/#8)

**Files:** Create `supabase/migrations/<ts>_colaborador_banco.sql`; Modify `src/modules/cadastros/colaboradores/{schemas,queries,actions}.ts` e `components/colaboradores-form-drawer.tsx`; `src/lib/database.types.ts` (regen).

- [ ] **Step 1: Migration** — `alter table public.colaboradores add column if not exists banco text, add column if not exists agencia text, add column if not exists conta text, add column if not exists tipo_conta text, add column if not exists chave_pix text; alter table public.colaboradores add constraint colaboradores_tipo_conta_check check (tipo_conta is null or tipo_conta in ('corrente','poupanca'));` (`salario`/`valor_diaria` já existem). Aplicar via `apply_migration`, `get_advisors`, `generate_typescript_types` → atualizar database.types.ts. Rollback documentado.

- [ ] **Step 2: Schema/queries/actions** — LER `colaboradores-form-drawer.tsx`/`schemas.ts` primeiro. No schema Zod: `salario`/`valorDiaria` opcionais (dinheiro, 2 casas — reusar o validador de dinheiro do projeto, ex. o de compras); `banco`/`agencia`/`conta`/`chavePix` texto opcional; `tipoConta` opcional ('corrente'|'poupanca'). queries/actions passam a ler/gravar os campos novos (mapear camelCase↔snake_case como o resto do form).

- [ ] **Step 3: Form** — adicionar duas seções (usar `SecaoFormulario` + `LinhaCampos`): **Remuneração** (salário, valor da diária — Input decimal `tabular-nums text-right`, como os campos de dinheiro da OC) e **Dados bancários** (banco, agência, conta, tipo de conta via `Combobox` corrente/poupança, chave PIX). Preserva todos os campos atuais.

- [ ] **Step 4: Verificar** — em banco: preencher `salario` num colaborador CLT ativo com apontamento aprovado e gerar a folha (`fn_gerar_folha`) → o item da folha NÃO sai mais R$ 0 (salário_base/custo preenchidos). Portão: `typecheck/lint/test/build`. Commit: `feat(rh): salário, diária e dados bancários no cadastro de colaborador`.

---

## Task 2: Anexos em ASO/EPI/atestado (#12)

**Files:** Create `supabase/migrations/<ts>_anexos_rh.sql`; Modify as telas de `rh/documentos`, `rh/epis`, `rh/ocorrencias` (components) + suas queries/page (pra `anexosIniciais`).

- [ ] **Step 1: Ler ao vivo** — `pg_get_functiondef` de `fn_recurso_do_anexo` e da fn do path (`fn_recurso_do_anexo_do_path` ou similar — ver `supabase/migrations/20260618201716_compras_anexos_storage.sql`). Confirmar os recurso ids de RH em `config/recursos.ts` (`rh.documentos`, `rh.epis`, `rh.ocorrencias` — nomes exatos). Ler como cada tela de RH (documentos/epis/ocorrencias) é estruturada (só form-drawer? tem detalhe?) pra saber ONDE cai o `AnexosRegistro`.

- [ ] **Step 2: Migration** — `create or replace` da(s) fn(s) `fn_recurso_do_anexo` (e a do path) ADICIONANDO os casos: `rh_documentos → 'rh.documentos'`, `rh_epis → 'rh.epis'`, `rh_ocorrencias → 'rh.ocorrencias'`, preservando TODOS os casos de compras existentes. Assim a RLS de `anexos` e as policies de `storage.objects` passam a cobrir os anexos dessas tabelas, gated pela permissão da aba. Aplicar via MCP; `get_advisors`. Rollback = a versão anterior da fn.

- [ ] **Step 3: Ligar o AnexosRegistro** — nas telas de documentos/EPI/ocorrências, colocar `<AnexosRegistro tabela="rh_documentos"|"rh_epis"|"rh_ocorrencias" registroId={registro.id} podeEditar={pode} anexosIniciais={...} />` para o registro existente. Buscar `anexosIniciais` no server (`listarAnexos(tabela, id)`) e passar (padrão do #10). Se hoje só há form-drawer de criar/editar (sem detalhe), colocar o AnexosRegistro no drawer quando for edição (registro já tem id), OU numa seção de detalhe — decidir conforme o que o Step 1 achou e explicar.

- [ ] **Step 4: Verificar** — RLS: com `rh.documentos` ver, lista; anexar exige `criar`/`editar` do recurso. Em banco/preview: anexar um PDF num documento/ASO aparece. Portão. Commit: `feat(rh): anexar arquivo em ASO, EPI e atestado`.

---

## Task 3: Ficha unificada do colaborador (#13)

**Files:** Create `src/app/(app)/cadastros/colaboradores/[id]/page.tsx`, `loading.tsx`; `src/modules/cadastros/colaboradores/components/ficha-colaborador.tsx` + queries de resumo por colaborador; Modify a tabela de colaboradores (linkar a linha).

- [ ] **Step 1: Ler** a tabela/lista de colaboradores (`colaboradores-tabela.tsx`) e as queries das fontes de RH (rh_apontamentos, rh_ferias, rh_documentos, rh_epis, rh_ocorrencias, rh_adiantamentos, rh_diarias) pra reusar shapes/nomes.

- [ ] **Step 2: Queries de resumo** — funções por colaborador (enxutas: últimos N + contagem/alertas, não a lista inteira): ponto recente, férias (período/limite/alertas), documentos/ASO (vencidos/a vencer), EPI (entregues/pendentes de devolução), ocorrências (recentes), adiantamentos (em aberto), diárias (recentes). Em `queries.ts` do módulo colaboradores (ou um `ficha.ts`).

- [ ] **Step 3: Rota + UI** — `[id]/page.tsx` (Server Component: checa `cadastros.colaboradores` ver, carrega colaborador + resumos, cada bloco só se o usuário tem `ver` no recurso de RH correspondente via `temPermissao`) + `loading.tsx` (SkeletonPagina). `ficha-colaborador.tsx`: `PageHeader` (nome/função/vínculo/obra + salário/diária/banco se `cadastros.colaboradores` ver), e `SecaoDetalhe`/cards por bloco com resumo + link "ver tudo" pra aba do RH. **Read-only.** Linkar a linha da tabela de colaboradores pra `/cadastros/colaboradores/[id]`.

- [ ] **Step 4: Portão + validação local** `typecheck/lint/test/build`; `npm run dev` (ver a ficha). Commit: `feat(cadastros): ficha unificada do colaborador`.

---

## Task 4: Verificação final + preview

- [ ] **Step 1: Portão final** (limpar `.next`) `typecheck/lint/test/build` verde; `get_advisors` sem novo.
- [ ] **Step 2: Preview** push; roteiro: preencher salário/banco num colaborador → folha calcula; anexar ASO/EPI/atestado; abrir a ficha do colaborador.
- [ ] **Step 3: Merge após OK do Tiago** `git checkout main && git merge --no-ff feat-rh-gestao ... && git push origin main`.

---

## Self-review (feito ao escrever)
- **Cobertura do spec:** salário/diária+banco no cadastro (T1, destrava folha); anexos RH via extensão do fn_recurso_do_anexo + AnexosRegistro (T2); ficha unificada read-only (T3); verificação (T4). Fora de escopo (folha oficial etc.) não entra. Coberto.
- **Placeholders:** fns vivas (fn_recurso_do_anexo + path) e as telas de RH têm "ler antes"; `<ts>` = timestamp. Sem TODO solto.
- **Consistência:** colunas banco (T1) usadas na ficha (T3); recursos rh.documentos/epis/ocorrencias no fn_recurso_do_anexo (T2) batem com os recursos de RH; AnexosRegistro reusa o padrão do #10.
